import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function LibraryPage() {
  return (
    <PlaceholderPage
      eyebrow="Content - Library"
      title="Asset library"
      description="Lead magnets, case studies, decks, sales assets - all in one place."
      comingNext="Lead Magnet Manager + Sales Asset Library (Plans 5, 9)"
    />
  );
}
