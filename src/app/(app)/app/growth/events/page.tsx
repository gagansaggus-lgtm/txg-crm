import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function EventsPage() {
  return (
    <PlaceholderPage
      eyebrow="Growth - Events"
      title="Events"
      description="Trade shows, conferences, speaking calendar, lead capture."
      comingNext="Trade Show & Speaking Calendar (Plan 8)"
    />
  );
}
