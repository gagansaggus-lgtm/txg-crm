import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SalesKitPage() {
  return (
    <PlaceholderPage
      eyebrow="Strategy - Sales Kit"
      title="Sales enablement"
      description="Pitch decks, battle cards, ROI calculator, proposal templates."
      comingNext="Pitch Asset Library + Battle Cards + Proposal Generator (Plan 9)"
    />
  );
}
